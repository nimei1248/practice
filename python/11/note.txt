1.冒泡排序
arr = [1,2,3,7,6,9,2,3,2,11,7]

def quick_sort(arr):
    如果arr长度是1 return
    找一个标志位 ## 可任意找一个标志位
    比标志位小的,放在左边
    比标志位大的,放在右边
    return quick_sort(左边的) + 标志位 + quick_sort(右边的)

2.快速排序

3.爬虫
requests

